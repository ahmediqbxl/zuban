node_modules/
.svelte-kit/
build/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
static/audio/generated/

# Review working files. The CSV is a scratch hand-off to a reviewer; the
# resulting review.json IS committed, because it is the record of who
# approved what.
content/*/review.csv
content/*/review-*.csv
